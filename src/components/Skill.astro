---
interface Props{
  name:string;
  image:string
}

const {name,image}= Astro.props;
---

<div class="flex flex-col items-center gap-4">
  <img src={image} class="w-20" alt="">
  <span class="">{name}</span>
</div>
